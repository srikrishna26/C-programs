#include <stdio.h>
#include <string.h>
int main() 
{
    char a[10];
    int n,p;
    int j;
    gets(a);
    scanf("%d",&n);
    scanf("%d",&p);
    
    for(j=p;a[j]!='\0';j++)
    {
     a[j]=a[j+n];
    }
    puts(a);
    
    return 0;
}
